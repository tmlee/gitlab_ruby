require "spec_helper"

RSpec.describe GitlabRuby do
  it "has a version number" do
    expect(GitlabRuby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
